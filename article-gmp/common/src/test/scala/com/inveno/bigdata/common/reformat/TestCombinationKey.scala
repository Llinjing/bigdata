package com.inveno.bigdata.common.reformat

object TestCombinationKey {
    def main(args: Array[String]) {
				println(CombinationKey.makeArticleCombinationKey("###", "content_id", "product_id"))
    }
}